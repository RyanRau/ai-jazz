name: Test Cleanup

on:
  pull_request:
    types: [closed]
    branches: [main]

# Shared with Build and Deploy / Test Deploy — see the note there.
concurrency:
  group: droplet
  cancel-in-progress: false

jobs:
  # Build and Deploy also clears test state, but only when it actually deploys.
  # A merge that touches no app (docs, workflows) skips that step, so this job
  # is the guarantee that a merged branch never leaves a test container running.
  cleanup:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Remove test deployment
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: deploy
          key: ${{ secrets.DROPLET_SSH_KEY }}
          script: |
            set -e
            cd /opt/apps

            # Check if a test deployment is active
            if [ ! -f test-deploy.active.yml ]; then
              echo "No active test deployment, nothing to clean up."
              exit 0
            fi

            echo "Cleaning up test deployment..."

            # Remove the test config
            rm test-deploy.active.yml

            # Pull latest main
            git pull origin main

            # Regenerate compose without test overlay
            python3 infra/generate-compose.py

            # Redeploy — --remove-orphans drops the test container
            docker compose up -d --remove-orphans

            # Clean up old images
            docker image prune -f

            echo "=== Test cleanup complete ==="

  remove-test-deploy-file:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Remove test-deploy.yml if present
        run: |
          if [ ! -f test-deploy.yml ]; then
            echo "No test-deploy.yml on main, nothing to remove."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git rm test-deploy.yml
          git commit -m "chore: remove test-deploy.yml after merge"
          git push
          echo "=== test-deploy.yml removed from main ==="
